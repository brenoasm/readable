export const getPosts = ({posts}) => posts;
export const getPost = ({post}) => post;
export const getFormProperties = ({formProperties}) => formProperties;
export const getSelectedPostId = ({post}) => post.id;
